# В dev-окружении выставлен NODE_ENV=production, из-за чего npm по умолчанию
# пропускает devDependencies (omit=dev) и ломает tsc/eslint/vitest.
# Принудительно ставим dev-зависимости; продакшен-сборки должны использовать
# `npm ci --omit=dev` явно.
include=dev
